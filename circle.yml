test:
  pre:
    - pyenv global 3.5.0
    - python --version
    #When using pyenv global 3.5.0 python2 doesn't work any longer
    #- python2 --version
    - python3 --version

  override:
    - python3 -c 'print("OK")'
