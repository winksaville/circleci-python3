# Circle.yml for testing python3 with Ubuntu 14.04
machine:
  python:
    version: 3.5.1

test:
  pre:
    #- pyenv global 3.5.1
    - python --version
    - python2 --version
    - python3 --version

  override:
    - python3 -c 'print("OK")'
